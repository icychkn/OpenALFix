#!/bin/bash

# expectations
# $PROJECT_DIR points to the root of the project directory
# $DOT_MINECRAFT points to the location of the .minecraft directory to install the mod

cp $PROJECT_DIR/build/libs/*-1.0.0.jar $DOT_MINECRAFT/mods/